<div class="cube">
    <img class="cube" src="{{imageUrl}}">
    <p class="name">{{name}}</p>
    <p><span>Power:</span> {{power}}</p>
    <a class="btn btn-primary" href="/pokemons/details/{{_id}}">Details</a>
    <a class="btn btn-primary" href="/pokemons/{{_id}}/attach-element">Attach Element</a>
</div>